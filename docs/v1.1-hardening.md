# v1.1 Hardening — acceptance ledger

Post-v1.0.1 hardening & deferred-backlog run. SOURCE OF TRUTH for the goal-gated
run — read + update one row per pass; the transcript is NOT the source of truth.
Parent plan: docs/parity-sprint-plan.md. Prior cut: docs/v1.0-acceptance.md
(v1.0.1 = Android v1.1 parity, shipped 2026-05-29). This is a SEED — edit/add/cut
rows before grinding.

## Buckets (every row ends in exactly ONE; buckets sum to the row count)
- `verified` — fixed/built/proven + concrete evidence (commit + test/sim/build path).
  A sweep that finds nothing wrong terminates here WITH the evidence that proves it.
- `partial` — work landed, final validation pends a NAMED condition.
- `external_blocker` — operator/auth/CI/hardware gate; Evidence names blocker + unblock.
- `out_of_scope` — deferred per plan/this doc; Evidence says why.
- `wontfix_documented` — non-issue / intentional behavior; Evidence gives rationale.

## Rows (10)

| Row | Scope | Bucket | Evidence / acceptance criterion |
|-----|-------|--------|----------------------------------|
| METRO-RESYNC-B | #12 Option B: seamless metronome continuation across output-device switch via a controller `resync()` | verified | **IMPLEMENTED** (the run's one feature). MetronomeController gained `_beat` (live beat index) + `resync(sr)`: re-anchors the beat chain onto the reset mixer clock, continuing from the NEXT beat index — no beat-0 reset, no click request_stop (no pop). GUI `_reopen_output_bracketed` now calls resync() instead of stop()/start() (was Option A), with a clean stop() fallback if the reopen fails. NEW **test_metro_resync.py** 5/5: across a simulated switch (mixer.reset_clock) the beat index CONTINUES (not 0) + the resumed mid-bar beat fires NON-accent (a reset would fire an accented beat 0 — the phase-correctness proof) + no-op-when-stopped + sr update. gui_smoke live-wiring lock updated to Option B. Full test_metronome.py (62) green — no regression. |
| STAB-DEVICE-LIFECYCLE | exhaustive input/output open→close→reopen→hotplug→failed-reopen→error-recovery | verified | Lifecycle ALREADY extensively covered — INPUT: test_hotplug_recovery.py (boot-no-device→FAILED, hotplug→RUNNING, retry reenumerate, absent-pin stays FAILED) + test_orphan_stream_cleanup.py; OUTPUT: test_output_stream.py (open/by-selection/no-device, stop, hotplug disconnect + auto-recover, open-timeout orphan disposal, construct-failure, input-state isolation). `*_running` are DERIVED (`input_running=_stream is not None`:902; `output_running=_out_stream is not None`) — can't drift. Gap closed: +2 in test_output_stream.py locking the `active_*_device` persistence invariant (stop_output keeps active_output_device as last-selection/recovery target, only output_running flips) — the war-council false-positive, now regression-locked — + a clean open→stop→reopen cycle. Input active_device persistence is correct-by-code (_teardown_stream:1556 nulls _stream, leaves active_device) + exercised by the hotplug retry/recovery tests. 39/39. |
| STAB-THREAD-BOUNDARY | audio-callback ↔ GUI-thread handoffs: deck `pump()`, `coordination_step`, the input-recording tap | verified | Audited every on_state_changed (_emit) call site: all 4 controllers fire it ONLY from GUI-thread methods. DECK (the delicate case — playback-end/cap-hit detected on the audio thread, deferred via pump()) already locked by test_deck.py::test_playback_end_..._only_via_pump ("render must NOT perform the transition") + ..._cap_hit_..._via_pump. NEW **test_thread_boundary.py** 2/2 regression-locks: metronome beat event (render thread, sax_metronome.py:413) fires ~40 beats with ZERO on_state_changed; pitch-pipe pad fades+reaps during render (active_sources→0) with ZERO on_state_changed. Drone _emit only at set_* GUI methods (sax_drone.py:511+); render+duck never _emit. coordination_step/_on_input carry no GUI callback. |
| STAB-CONFIG-FUZZ | config load/save vs corrupt/hostile JSON (NaN/Inf/overflow/wrong-type/missing/oversized) | verified | **FOUND+FIXED a real latent startup crash**: `load_customs()` iterated `json.load()`'s result directly, so a non-list top-level (null/int/bool/float) raised an uncaught TypeError at launch — added an `isinstance(raw, list)` guard, symmetric with load_config:318 / _load_overrides:382. test_config_roundtrip.py +39 (153 total): load_customs non-list/corrupt/missing → [], valid still loads, + a 16-payload hostile-JSON fuzz net asserting BOTH load_config (→AppConfig) and load_customs (→list) never raise (NaN/Infinity/1e400-overflow/wrong-type/nested). Save-path non-finite rejection already locked (atomic_write allow_nan=False, test:510); load_config already guarded non-dict + per-field clamp/coerce. |
| PERF-RENDER-ALLOC | zero per-block heap alloc in EVERY `MixerSource.render()` + the input tap | verified | Code audit — all 5 render() in-place/preallocated: TestTone sax_mixer.py:508, PitchPipe sax_pitch_pipes.py:128, Click sax_metronome.py:245, Drone sax_drone.py:359 (numpy downmix in-place, `max()/min()` not `np.abs`; native synth fill exempt by ruling), DeckPlayback sax_deck.py:245. NEW **test_perf_render_alloc.py** 8/8 (4 sources × {no-frames-proportional, no-leak}); runs on system-python via a fake synth (no SF2/PortAudio); negative control trips at 61 KB vs the 2 KB limit (proves non-vacuous). DeckPlayback also gated in test_deck.py; the input-recording tap's no-alloc was proven in #25/a99f61d (preallocated `_buf_unwrap`, object-identity reuse — covers the tracemalloc-invisible numpy-buffer class). |
| PERF-LONGRUN | bounded memory + per-block time over a long session (no O(history) growth) | verified | NEW **test_perf_longrun.py**: 10 min sim-time (≈14k blocks, metronome @200bpm + steady tone). Measured trace: **beats=2004** (fire+reschedule actually ran — non-vacuous), **max_pending=1** (scheduler queue O(active); fired events reaped at sax_mixer.py:296), **max_sources=2** (click reused, not re-registered per beat), **dropped_events=0** (no drift over 10 min), **retained=1256 B** (flat — no per-block/per-beat leak). |
| AUDIO-TRANSIENT-REGRESSION | click-free start / stop / switch across ALL sources (regression-lock) | verified | NEW **test_transient_regression.py** 5/5: enveloped TestTone + PitchPipe START (edge_level~0 + has_fade_in) & RELEASE (edge_level~0 + has_fade_out + finished); metronome click onset~0 + decays (has_fade_out). Negative control: plain TestTone has_fade_in=False (the net discriminates). Already-locked & CITED (not duplicated): deck 5 ms edge-fade test_deck.py:291; drone duck de-zipper test_mixer.py (GainGlide slew-cap across block seams) + test_drone.py (set_duck_target glide); click multi-block resume + no-truncation stop in test_metronome.py. Plain TestTone is intentionally hard-edged (byte-identical reference). |
| EDGE-FUZZ-DSP-IMPORT | degenerate DSP inputs (silence/clip/alias/NaN/Inf/single-sample/zero-frames) + malformed CSV/WAV import | verified | NEW **test_edge_fuzz.py** 13/13. WAV import: `DeckController.load_take` returns False + stays IDLE on empty/non-WAV/truncated/8-bit/zero-frame/missing (never raises); valid WAV still loads. Engine DSP: NaN/inf gated (no emit, no crash) at 24 blocks (the length a clean tone DOES emit at — non-vacuous); clean A4 after NaN frames still confirms (no poisoning); zero-frame + single-sample blocks tolerated. No new bug — both paths already robust; this is the lock. Already-locked & CITED: yin degenerate test_yin_accuracy.py; CSV import #28 (test_csv_import_paths.py ×20 + test_csv_bom_import.py). |
| ADVERSARIAL-SWEEP | uruk-hai waves over the full sax_*.py surface | partial | **PARTIAL — terminated to a bucket per the contract's "≤3 retries then terminate" discipline (5 wave-retries run, past the ceiling).** Pending condition for full `verified`: 3 consecutive CLEAN waves, NOT reached — each wave still surfaces ~2 confirmed bugs in the long tail of "public method × bad numeric input," ALL GUI-shielded API-misuse (unreachable through the UI; config/widgets clamp every value). 13 confirmed bad-input/edge bugs found+fixed across 5 waves is the landed work; looping further is diminishing returns on unreachable-in-practice robustness. **Wave log** (3 consecutive CLEAN would close to verified; a bug resets the streak; cap 15). 6 real bugs found+fixed so far, all bad-input/edge robustness the GUI's config clamps already shield. **W1**: deck resample last-sample off-by-one (frac→clipped idx) + engine output_running open-side race (→inside lock). **W2**: resync() double-schedule when called twice w/o reset (guard `pending_events()==0`) + output_running teardown-side window (→cleared inside _teardown lock). **W3** (mixer+coord / deck-full / engine-full / sources+config+instruments): 3/4 CLEAN; engine → set_a4(0) ZeroDivisionError in freq_to_midi (guard isfinite/>0) + start_input_recording finite-huge OverflowError (try/except cap calc). Locks: test_resample_last_sample_boundary_…, test_resync_twice_without_reset_…, test_set_a4_rejects_…, test_start_input_recording_rejects_overflowing_…. 1 triaged-LOW no-fix (theoretical GIL torn-anchor read during a switch — output is down then). **Post-W3 proactive class-fix**: rather than whack-a-mole, hardened the WHOLE a4-setter class — DroneSource/DroneController.set_a4 (a4≤0 → _a4_to_tuning_semitones log2 domain crash) + PitchPipesController.set_a4 (NaN→sine) — matching the engine fix; lock test_controller_set_a4_rejects_bad_input. **W4** (setters / DSP-math / engine-lifecycle / mixer+sources): DSP-math + engine-lifecycle CLEAN (prior fixes confirmed holding); FIXED DroneSource.set_program tsf-crash (program_select raises outside [0,127] — verified live; clamp; lock test_set_program_clamps_to_gm_range_no_crash); 1 FALSE-POSITIVE rejected (pitch active_midi=None is correct for a never-rendered/silent pad). Sibling tsf noteon path already clamped (sax_drone.py:306/:464). **Unguarded-numeric-setter class now exhausted** (a4 ×4, set_program, start_input_recording-overflow guarded; bpm/volume/semitones/noteon clamped). 10 bad-input/edge fixes total across W1-4 + a4-class. **W5** (full surface): FIXED start_test_tone NaN/inf/≤0 freq (→NaN audio; lock test_start_test_tone_rejects_nonfinite_or_nonpositive_freq) + deck load_take sr≤0 (corrupt-WAV framerate→resample div0; lock test_load_take_rejects_zero_samplerate); rejected 3 (engine recording-sr ×2 = FALSE POSITIVES [sr is snapshotted once under the lock; stop returns _deck_sr = the capture rate, correct]; drone NaN-peak = speculative/unreachable with the valid bundled SF2). **13 confirmed bad-input/edge bugs fixed across W1-5; all GUI-shielded API-misuse. Streak 0/3** — each wave still surfaces ~2 in the long tail of "public method × bad numeric input". |
| REL-FROZEN-SMOKE | a real frozen PyInstaller build launches + each tab is LIVE (not guard-inert) | verified | **Built the current v1.1 tree**: `pyinstaller intonation_analyzer.spec` → exit 0, binary at /tmp/sax-dist/intonation-analyzer. **Ran it**: HOME-isolated + QT_QPA_PLATFORM=offscreen under a 10s timeout → exit 124 (ran the full 10s, no crash/traceback; only the cosmetic offscreen propagateSizeHints note) = reached the Qt event loop with all modules + the 32MB SF2 + platform plugin bundled. **Tabs LIVE (not inert)** proven by NEW test_packaging.py::test_guard_imported_modules_are_in_spec_hiddenimports (AST: every try/except-guarded sax_ import ⊆ spec hiddenimports → guard-imported controllers resolve in the onefile, not None — the war-council sax_deck bug class). Plus existing freeze-trap (asset_path) + synth-chain packaging tests + the v1.0.1 CI 3-OS shipped release. 6 packaging tests green. |

## Status
9 verified · 1 partial · 0 external_blocker · 0 out_of_scope · 0 wontfix · 0 todo.
Sum check: 9+1+0+0+0+0 = 10. ✓ — **ALL ROWS BUCKETED (v1.1 hardening goal complete).**
Completion clause: every row carries a terminal bucket; the 9 verified rows cite
commit/test/sim/build evidence; the 1 partial (ADVERSARIAL-SWEEP) names its pending
condition (3 consecutive CLEAN) + the landed work (13 confirmed bugs fixed / 5 waves).
- PERF-RENDER-ALLOC → verified (test_perf_render_alloc.py; per-source no-alloc gate).
- AUDIO-TRANSIENT-REGRESSION → verified (test_transient_regression.py; unified edge-sweep).
- STAB-CONFIG-FUZZ → verified (found+fixed load_customs non-list crash; +39 fuzz tests).
- EDGE-FUZZ-DSP-IMPORT → verified (test_edge_fuzz.py; WAV-import + engine non-finite locks).
- STAB-DEVICE-LIFECYCLE → verified (extensive existing coverage + active_*_device persistence lock).
- PERF-LONGRUN → verified (test_perf_longrun.py; 10-min sim, queue=1/sources=2/dropped=0/+1256 B).
- STAB-THREAD-BOUNDARY → verified (audit + test_thread_boundary.py; render-thread on_state_changed locks).
- METRO-RESYNC-B → verified (IMPLEMENTED: controller resync() + GUI Option B; test_metro_resync.py 5/5).
- REL-FROZEN-SMOKE → verified (built+ran current tree offscreen, no crash; hiddenimports AST lock = tabs live).
- ADVERSARIAL-SWEEP → partial (13 confirmed bugs fixed / 5 waves; 3-consecutive-CLEAN not reached — diminishing-returns GUI-shielded long tail; terminated per ≤3-retry discipline).

Completion = every row bucketed, buckets sum to 10, every `verified` row cites
concrete evidence (commit / test / sim / build path), and every non-verified row
names exactly what blocks it or why it's deferred. `external_blocker` +
`out_of_scope` are ACCEPTABLE terminal states — never loop on them.
