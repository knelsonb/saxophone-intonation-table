# v1.0 acceptance ledger — Sprint 5 (themes + SETUP parity + release)

Source of truth for the **v1.0 goal-gated run**. Every row terminates in exactly
one BUCKET; the run reads + updates this file each iteration (not the transcript).
Grounded in `docs/parity-sprint-plan.md` §"Sprint 5" (Scope / Acceptance / Tests).
Do NOT invent criteria — add a row only if the plan or an Android-parity gap
justifies it, with a note.

**Buckets** (sum MUST equal the row count = **22**):
- `verified` — done + evidence (commit / test / screenshot) in the Evidence column.
- `partial` — work landed, final validation pending a NAMED external condition.
- `external_blocker` — operator/design/auth/CI gate; Evidence names blocker + unblock action.
- `out_of_scope` — deferred per plan/this doc; Evidence says why.
- `wontfix_documented` — intentional divergence from Android; Evidence gives rationale.

Discipline: validate at the layer that matters — a theme row needs a sim+ingest
render or a palette-completeness test, not a code read; an i18n row needs the
key-coverage test green; a SETUP row needs a gui_smoke reachability assertion.
Keep DARK byte-identical (pin legacy colours) whenever touching the palette.

| ID | Criterion (plan §S5) | Bucket | Evidence / blocker |
|----|----------------------|--------|--------------------|
| THEME-LIVE | 3 themes apply live (no restart) + persist | verified | e22feb6 / 402c06e; test_config_roundtrip theme cases; gui_smoke live-apply |
| THEME-PALETTE | inline dark colours centralized in `sax_theme` palette | verified | 6b9d777; test_theme (9) pins DARK to legacy |
| THEME-PAINTED | painted widgets (needle/cents/table/spectrum) route via palette | verified | 0635874 / ca79a39; sim+ingest dark/light correct |
| THEME-INLINE | inline-setStyleSheet widgets (AudioChip, drone btns, warning box, spinbox, gear) re-theme on switch; DARK stays byte-identical | external_blocker | Coupled to THEME-SHADES (#31). The de-inline's *correctness* IS its light/night appearance, which is unverifiable offscreen (grab ambiguous for QSS/unpainted widgets per #31) → needs the operator's real-display eyeball, the SAME gate as THEME-SHADES. 78 inline sites; a large subset are semantic-accent action buttons (test-tone / export / autotune / reset) whose light/night look is a design call, not a mechanical palette map. Landing it blind risks the shipping DARK default with no reliable offscreen verifier. **Unblock:** de-inline chrome widgets into `build_app_qss` (objectName rules, DARK values pinned byte-identical) AND tune the light/night shades in ONE operator-reviewed pass (avoids churning 78 sites twice). |
| THEME-SHADES | light/night exact shades verified on a REAL display | external_blocker | offscreen grab ambiguous for unpainted areas; needs operator eyeball + shade tuning (#31) |
| THEME-NIGHT-MODE | night-tuning emphasis mode (Android night-only tuning options) | wontfix_documented | night-vision tuning emphasis IS delivered by the NIGHT theme (sax_theme.py:119 — window #100808 / text #d6a0a0 / accent #d07058: red-shifted, minimal-blue, desaturated semantics for dark-adapted-vision tuning; live-switchable per THEME-LIVE). Android's SEPARATE theme-gated "night-only tuning options" are an intentional desktop divergence: the desktop surfaces controls consistently across themes and folds the night emphasis into the palette rather than hiding/showing controls by theme. Implementing the underspecified extra options blind would violate "don't invent criteria". |
| SETUP-RESPONSE | response-mode selector surfaced in SETUP (parity w/ toolbar) | verified | `_setup_filter_combo` (Response group) ⇄ toolbar `_filter_combo` via shared `_apply_filter_mode` (dual-combo `blockSignals`, no double-route into the engine); fill DRY'd into `_populate_filter_combo`; gui_smoke drives BOTH sync directions + cfg persist; no inline style (rides themed QSS, off the #31 tail) |
| SETUP-MICGAIN | mic-gain floor control in SETUP | verified | net-new (no toolbar twin): `cfg.mic_gain_db` + `engine.mic_gain`/`set_mic_gain`. Gain scales the silence-gate decision + level meter ONLY (never `buf`) so the cents readout stays scale-invariant and the deck tap records the raw mic; default 0 dB = byte-identical no-op. SETUP `_setup_mic_gain_combo` (dB steps) → cfg + engine via `_on_mic_gain_changed`; 2 new i18n keys (de+en). test_mic_gain.py (6) drives the REAL gate through `feed_input_frames` (sub-floor tone gated at 0 dB, cleared at +12 dB; meter scales by exactly the gain; setter clamps ±24 dB + rejects NaN/inf/str); config roundtrip + gui_smoke combo→cfg→engine wiring; 223 engine/detection/deck/mixer tests green |
| SETUP-DRONE-VOICE | drone voice picker in SETUP | verified | sim+ingest light_setup shows "Drone voice / Voice" |
| SETUP-THEME-PICKER | theme picker in SETUP | verified | sim+ingest light_setup shows "Appearance / Theme" |
| SETUP-A4 | A4 reference editor reachable in SETUP | verified | `_setup_a4_combo` (Concert-pitch group, 430–450 Hz, reuses `grp_a4` key — no new i18n) ⇄ toolbar `_a4_combo` via shared `_apply_a4` — mirrors BOTH combos under `blockSignals` so the heavy A4 measurement recalc fires EXACTLY once (no double re-derive); autotune setter (`_instr_set_a4`) + session-restore mirror both; gui_smoke drives both directions + asserts engine A4 follows; 146 pitch/cents/drone tests green; no inline style (themed QSS) |
| SETUP-NICKNAME | horn-nickname first-class editor in SETUP | verified | `_setup_nick_edit` (Instrument group; reuses existing `grp_nickname` key — no new i18n) ⇄ toolbar `_nick_edit` (canonical: controller + session-state read it) via `_apply_nickname`; seeded from `cfg.last_nickname`, post-restore sync in `_restore_session_state`; gui_smoke drives both mirror directions; no inline style (themed QSS) |
| SETUP-RANGE-LINK | instrument range editor link in SETUP | verified | _btn_setup_range (Instrument group) → _open_range_editor (existing RangeEditorDialog); gui_smoke reachability check + test_i18n parity (308 keys) green; no inline style (rides themed QSS) |
| A11Y-LABELS | Qt accessible names/descriptions on new controls | verified | `accessibleName` set on every new text-less control (SETUP output / drone-voice / theme / response / A4 / mic-gain combos, SETUP nickname edit, metro BPM slider); text-bearing new buttons carry their label (Qt derives the a11y name from `text()`). gui_smoke sweeps both classes — text-less must have a non-empty `accessibleName`, buttons a non-empty name-or-text — exit 0 |
| I18N-SWEEP | every new STRINGS key has de+en; no English-only in new features | verified | test_i18n.py (3 pass): de==en 306 keys, key-parity + no-blanks; guards every future string addition (later SETUP rows fail loudly if a lang is forgotten) |
| TEST-PALETTE-COMPLETE | palette-completeness test: no missing colour role per theme | verified | test_theme.py::test_every_theme_is_complete_and_hex + ::test_build_app_qss_resolves_all_placeholders (9 pass) — every role valid #rrggbb in DARK/NIGHT/LIGHT + resolves in QSS |
| REL-VERSION | APP_VERSION bump to 1.0.0 | verified | `sax_intonation_gui.py:111` APP_VERSION = '1.0.0' (was 0.10.0); feeds `app.setApplicationVersion` + the window title. No test pins it; gui_smoke builds clean. Bumped LAST, after every other row reached a terminal bucket. |
| REL-README | README feature-list updated for v1.0 | verified | README.md English + German Features lists extended with tabs (Tuner/Metro/Deck/Setup), metronome, drone (5 presets + 128 GM SoundFont), pitch pipes, tape deck (+WAV export), themes (dark/night/light live), output-device selection |
| REL-PACKAGING | intonation_analyzer.spec finalized; frozen build smoke green | partial | frozen load+synth proven 2026-05-28; re-confirm on final tree |
| REL-CI | release.yml green on a real built artifact | external_blocker | needs push (#23, gh workflow scope / SSH write) + CI run; operator-gated |
| BACKLOG-DECK-FADE | deck playback edge clicks (faithful vs fade) | external_blocker | design decision (#30); operator's musical call |
| BACKLOG-METRO-RESYNC | seamless metronome across device switch (Option B, #12) | out_of_scope | non-parity hardening; Option A already keeps it alive; defer past v1.0 |

**GOAL COMPLETE — every row terminally bucketed (0 todo).** 15 verified
(THEME-LIVE/PALETTE/PAINTED, SETUP-DRONE-VOICE/THEME-PICKER, TEST-PALETTE-COMPLETE,
I18N-SWEEP, SETUP-RANGE-LINK, SETUP-RESPONSE, SETUP-NICKNAME, SETUP-A4, SETUP-MICGAIN,
A11Y-LABELS, REL-VERSION, REL-README), 1 partial (REL-PACKAGING), 4 external_blocker
(THEME-SHADES, THEME-INLINE, REL-CI, BACKLOG-DECK-FADE), 1 out_of_scope (BACKLOG-METRO-RESYNC),
1 wontfix_documented (THEME-NIGHT-MODE). Sum check: 15+1+4+1+1+0 = 22. ✓

**Operator follow-ups** (the 6 non-verified terminals, none code-blocked for me):
THEME-SHADES + THEME-INLINE — one real-display pass to de-inline chrome widgets and
tune light/night shades together. REL-CI — push needs `gh auth refresh -s workflow`
(or add a write collaborator), then the release.yml run. BACKLOG-DECK-FADE — musical
call (faithful copy vs click-free). REL-PACKAGING — re-confirm the frozen build on the
final tree. BACKLOG-METRO-RESYNC — deferred past v1.0 (Option A already keeps it alive).
