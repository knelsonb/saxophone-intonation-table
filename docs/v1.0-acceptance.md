# v1.0 acceptance ledger — Sprint 5 (themes + SETUP parity + release)

Source of truth for the **v1.0 goal-gated run**. Every row terminates in exactly
one BUCKET; the run reads + updates this file each iteration (not the transcript).
Grounded in `docs/parity-sprint-plan.md` §"Sprint 5" (Scope / Acceptance / Tests).
Do NOT invent criteria — add a row only if the plan or an Android-parity gap
justifies it, with a note.

**Buckets** (sum MUST equal the row count = **22**):
- `verified` — done + evidence (commit / test / screenshot) in the Evidence column.
- `partial` — work landed, final validation pending a NAMED external condition.
- `external_blocker` — operator/design/auth/CI gate; Evidence names blocker + unblock action.
- `out_of_scope` — deferred per plan/this doc; Evidence says why.
- `wontfix_documented` — intentional divergence from Android; Evidence gives rationale.

Discipline: validate at the layer that matters — a theme row needs a sim+ingest
render or a palette-completeness test, not a code read; an i18n row needs the
key-coverage test green; a SETUP row needs a gui_smoke reachability assertion.
Keep DARK byte-identical (pin legacy colours) whenever touching the palette.

| ID | Criterion (plan §S5) | Bucket | Evidence / blocker |
|----|----------------------|--------|--------------------|
| THEME-LIVE | 3 themes apply live (no restart) + persist | verified | e22feb6 / 402c06e; test_config_roundtrip theme cases; gui_smoke live-apply |
| THEME-PALETTE | inline dark colours centralized in `sax_theme` palette | verified | 6b9d777; test_theme (9) pins DARK to legacy |
| THEME-PAINTED | painted widgets (needle/cents/table/spectrum) route via palette | verified | 0635874 / ca79a39; sim+ingest dark/light correct |
| THEME-INLINE | inline-setStyleSheet widgets (AudioChip, drone btns, warning box, spinbox, gear) re-theme on switch; DARK stays byte-identical | todo | sim+ingest light shows them dark-slate (task #31); de-inline into build_app_qss (objectName rules) |
| THEME-SHADES | light/night exact shades verified on a REAL display | external_blocker | offscreen grab ambiguous for unpainted areas; needs operator eyeball + shade tuning (#31) |
| THEME-NIGHT-MODE | night-tuning emphasis mode (Android night-only tuning options) | todo | confirm scope vs Android; impl or out_of_scope w/ rationale |
| SETUP-RESPONSE | response-mode selector surfaced in SETUP (parity w/ toolbar) | todo | not in SETUP per sim+ingest |
| SETUP-MICGAIN | mic-gain floor control in SETUP | todo | confirm Android has it; impl or wontfix |
| SETUP-DRONE-VOICE | drone voice picker in SETUP | verified | sim+ingest light_setup shows "Drone voice / Voice" |
| SETUP-THEME-PICKER | theme picker in SETUP | verified | sim+ingest light_setup shows "Appearance / Theme" |
| SETUP-A4 | A4 reference editor reachable in SETUP | todo | A4 in toolbar; surface in SETUP for parity |
| SETUP-NICKNAME | horn-nickname first-class editor in SETUP | todo | promote existing config field |
| SETUP-RANGE-LINK | instrument range editor link in SETUP | verified | _btn_setup_range (Instrument group) → _open_range_editor (existing RangeEditorDialog); gui_smoke reachability check + test_i18n parity (308 keys) green; no inline style (rides themed QSS) |
| A11Y-LABELS | Qt accessible names/descriptions on new controls | todo | gui_smoke asserts accessibleName() non-empty on new controls |
| I18N-SWEEP | every new STRINGS key has de+en; no English-only in new features | verified | test_i18n.py (3 pass): de==en 306 keys, key-parity + no-blanks; guards every future string addition (later SETUP rows fail loudly if a lang is forgotten) |
| TEST-PALETTE-COMPLETE | palette-completeness test: no missing colour role per theme | verified | test_theme.py::test_every_theme_is_complete_and_hex + ::test_build_app_qss_resolves_all_placeholders (9 pass) — every role valid #rrggbb in DARK/NIGHT/LIGHT + resolves in QSS |
| REL-VERSION | APP_VERSION bump to 1.0.0 | todo | sax_intonation_gui APP_VERSION |
| REL-README | README feature-list updated for v1.0 | todo | metro/drone/pipes/deck/themes |
| REL-PACKAGING | intonation_analyzer.spec finalized; frozen build smoke green | partial | frozen load+synth proven 2026-05-28; re-confirm on final tree |
| REL-CI | release.yml green on a real built artifact | external_blocker | needs push (#23, gh workflow scope / SSH write) + CI run; operator-gated |
| BACKLOG-DECK-FADE | deck playback edge clicks (faithful vs fade) | external_blocker | design decision (#30); operator's musical call |
| BACKLOG-METRO-RESYNC | seamless metronome across device switch (Option B, #12) | out_of_scope | non-parity hardening; Option A already keeps it alive; defer past v1.0 |

**State on resume:** 8 verified (THEME-LIVE/PALETTE/PAINTED, SETUP-DRONE-VOICE/THEME-PICKER,
TEST-PALETTE-COMPLETE, I18N-SWEEP, SETUP-RANGE-LINK), 1 partial (REL-PACKAGING), 3 external_blocker
(THEME-SHADES, REL-CI, BACKLOG-DECK-FADE), 1 out_of_scope (BACKLOG-METRO-RESYNC). 9 `todo` remain.
